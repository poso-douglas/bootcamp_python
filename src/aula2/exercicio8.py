# Desenvolva um programa que calcule a potência de um número (base e expoente fornecidos pelo usuário).
from math import pow

base = float(input("Insira o número base "))
exp = float(input("Insira o expoente "))

print(pow(base,exp))
